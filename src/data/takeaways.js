export const takeaways = {
  '01': [
    'DNS menerjemahkan domain ke IP — pahami urutan: browser → DNS → server',
    'HTTP methods: GET (baca), POST (buat), PUT/PATCH (ubah), DELETE (hapus)',
    'REST API menggunakan resource-based URLs dan status code standar',
    'CORS adalah mekanisme keamanan browser — beda origin perlu izin lewat header',
    'Client-server: frontend handle UI, backend handle logic & data',
  ],
  '02': [
    'Event Loop: Call Stack → Web API → Task Queue. Microtask (Promise) duluan sebelum macrotask',
    'Closure: fungsi bisa akses variable parent scope meski parent sudah selesai eksekusi',
    'Promise: pending → fulfilled/rejected. async/await adalah syntactic sugar',
    'Hoisting: deklarasi function & var diangkat ke atas scope-nya',
    'this tergantung cara fungsi dipanggil, bukan di mana didefinisikan',
  ],
  '03': [
    'Virtual DOM: perbedaan antara real DOM & VDOM, proses diffing & reconciliation',
    'Hooks: useState, useEffect, useRef, useMemo, useCallback — jangan panggil di dalam loop/if',
    'State management: useState (lokal), Context (global sederhana), Redux/Zustand (kompleks)',
    'Lifecycle component di React modern dikelola lewat useEffect dependency array',
    'React Router: SPA navigation tanpa reload, dukungan nested routes & loader',
  ],
  '04': [
    'MVC: Model (database), View (Blade), Controller (logic) — alur request response',
    'Eloquent ORM: Active Record pattern, relasi (hasMany, belongsTo), eager loading',
    'Middleware: filter request sebelum masuk controller (auth, CORS, rate limiting)',
    'Blade: template engine dengan @extends, @section, @include, @yield',
    'Migration & Seeder: version control schema database, isi data dummy',
  ],
  '05': [
    'JOIN: INNER JOIN (irisan), LEFT JOIN (semua kiri), RIGHT JOIN (semua kanan)',
    'Indexing mempercepat SELECT tapi perlambat INSERT/UPDATE — pakai di kolom yang sering di-query',
    'Normalization: 1NF (atomik), 2NF (no partial dependency), 3NF (no transitive dependency)',
    'ACID: Atomicity, Consistency, Isolation, Durability — jaminan transaksi',
    'Transaction: mulai commit/rollback — penting untuk operasi multi-tabel',
  ],
  '06': [
    'OOP: Encapsulation, Inheritance, Polymorphism, Abstraction',
    'Big O: O(1) < O(log n) < O(n) < O(n log n) < O(n²) — ukur efisiensi algoritma',
    'Struktur data: Array, Linked List, Stack, Queue, Hash Table, Tree, Graph',
    'Design pattern: Singleton, Factory, Observer, Strategy, Repository',
    'SOLID: Single Responsibility, Open-Closed, Liskov, Interface Segregation, Dependency Inversion',
  ],
  '07': [
    'JWT: stateless auth, tersimpan di client (localStorage/httpOnly cookie), perlu refresh token',
    'Session: stateful auth, tersimpan di server, pakai session ID cookie',
    'CORS di production: set Access-Control-Allow-Origin spesifik, bukan wildcard',
    'Upload file: validasi tipe & ukuran di client + server, simpan di cloud storage',
    'Deployment: build frontend → serve static, backend pakai environment variable',
  ],
  '08': [
    'Cheatsheet 1 halaman per topik — ringkasan paling penting untuk hafalan H-1',
    'Cocok dibaca ulang 30 menit sebelum interview sebagai final refresh',
    'Fokus ke sintaks, keyword, dan konsep yang sering ditanyakan',
    'Gunakan sebagai referensi cepat saat mengerjakan soal coding',
  ],
  '09': [
    'Baca soal dulu dengan teliti sebelum nulis kode — tanya kalau ada yang kurang jelas',
    'Tulis pseudo-code dulu kalau bingung, baru ubah ke sintaks JavaScript',
    'Pastikan handle edge case: input kosong, angka negatif, tipe data berbeda',
    'Latihan explain your thinking sambil nulis — interviewer menilai proses berpikir',
    'Gunakan nama fungsi & variabel yang deskriptif, bukan 1 huruf',
  ],
}
